{% extends 'base3.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Edit Users</h1>

        <div class="row">
            <div class="col-md-6">
                {{ include('admin1/_form.html.twig', {'button_label': 'Update'}) }}

            </div>
        </div>

        <div class="mt-3">
            <a href="{{ path('app_admin1_index') }}" class="btn btn-secondary">Back to List</a>
        </div>

        <div class="mt-3">
            {{ include('admin1/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

