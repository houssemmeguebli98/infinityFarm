<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome Page</title>
    <style>
        body {

            background-repeat: no-repeat;
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        .welcome-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative;
        }

        .welcome-container button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            margin-right: 10px;
            cursor: pointer;
        }

        .welcome-container button:hover {
            background-color: #45a049;
        }

        .welcome-container .auth-buttons {
            position: absolute;
            top: 20px;
            right: 20px;
        }
    </style>
</head>
<body>
<div class="welcome-container">
    <h1>Bienvenue dans INFINTYFARM</h1>
    <p>Découvrez notre contenu exceptionnel.</p>

    <div class="auth-buttons">
        <button onclick="location.href='{{ path('app_signup') }}'">Signup</button>
        <button onclick="location.href='{{ path('app_signin') }}'">Login</button>
    </div>
</div>
</body>
</html>
