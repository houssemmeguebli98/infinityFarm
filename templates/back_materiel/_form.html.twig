{{ form_start(form) }}

<div class="mb-3">
    {{ form_row(form.nommat, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="mb-3">
    {{ form_row(form.etatmat, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="mb-3">
    {{ form_row(form.quantitemat, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="text-center mt-4">
    <button type="submit" class="btn btn-primary">
        <i class="fas fa-save"></i> Enregistrer
    </button>
</div>

{{ form_end(form) }}
