<form method="post" action="{{ path('app_parc_delete', {'idparc': parc.idparc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parc.idparc) }}">
    <button class="btn">Delete</button>
</form>
