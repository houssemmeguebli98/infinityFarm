{% extends 'back.html.twig' %}

{% block title %}New Terrain{% endblock %}

{% block body %}
    <br>
    <br>
    <br>
    <br>
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h1 class="text-center m-0" style="font-size: 24px; color: white">Create new Terrain</h1>
                    </div>

                    <div class="card-body">
                        {{ include('backterrain/form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
        <br>
        <div class="text-center mt-4">
            <a href="{{ path('app_backterrain_index') }}" class="btn btn-primary mr-2">
                <i class="fas fa-arrow-left"></i> Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
