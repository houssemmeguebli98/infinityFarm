<form method="post" action="{{ path('app_terrain_delete', {'idterrain': terrain.idterrain}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terrain.idterrain) }}">
    <button class="btn">Delete</button>
</form>
