{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

<div class="mb-3">
    {{ form_row(form.nomterrain, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="mb-3">
    {{ form_row(form.localisation, {'attr': {'class': 'form-control'}}) }}
</div>

<div class="mb-3">
    {{ form_row(form.superficie, {'attr': {'class': 'form-control'}}) }}
</div>

<button type="submit" class="btn btn-primary">Enregistrer</button>

{{ form_end(form) }}
