<form method="post" action="{{ path('app_transaction_delete', {'idTra': transaction.idTra}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transaction.idTra) }}">
    <button class="btn">Delete</button>
</form>
